export type LivingSpace = {
    id: string;
    name: string;
    icon: string | null;
};

/** A selectable option of a Living Edit step. */
export type LivingEditOption = {
    id: string;
    name: string;
    icon: string | null;
};

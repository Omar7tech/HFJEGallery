export type LivingSpace = {
    id: string;
    name: string;
    icon: string | null;
};

export type StepOneOption = {
    id: string;
    name: string;
    icon: string | null;
};

export type StepTwoOption = {
    id: string;
    name: string;
    icon: string | null;
};
